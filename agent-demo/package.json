{
  "name": "agent-demo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --no-deprecation src/agent.mjs",
    "eval": "node --no-deprecation src/eval.mjs",
    "eval:quick": "node --no-deprecation src/eval.mjs read_basic fix_bug py_fix go_fix cross_lang_search",
    "clean": "rm -rf logs/*.log logs/*.json && echo 'logs 已清除'"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@repomix/tree-sitter-wasms": "^0.1.16",
    "@vscode/ripgrep": "^1.17.1",
    "dotenv": "^17.3.1",
    "ink": "^6.8.0",
    "react": "^19.2.4",
    "web-tree-sitter": "^0.26.7"
  }
}
