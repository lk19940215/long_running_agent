# requirements.md问题

- 错误内容：
    ```
    (.venv) E:\Code\ai-teaching-ppt\frontend>claude-coder run 

    ============================================
    Claude Coder
    ============================================

    [ERROR] 首次运行需要提供需求描述

    用法（二选一）:
    方式 1: 在项目根目录创建 requirements.md
            claude-coder run

    方式 2: 直接传入一句话需求
            claude-coder run "你的需求描述"

    (.venv) E:\Code\ai-teaching-ppt\frontend>cd ../           

    (.venv) E:\Code\ai-teaching-ppt>claude-coder run
    ```
- 错误原因：
    requirements.md 设计的初衷，是想让在代码循环的过程中，感知需求的变更。现在来看，是不是过渡设计？

- 改进思路
    是否废弃掉 add 模式？ 全部 run 来调用，add 改为 --add.
    如果保留，add 可以 add "需求" 以及 add -r 的模式，就知道有 requirements.md 文件存在，
    add 等模式，也需要能输出 " 思考中 时间 "这种信息。

# 路径识别

    windows电脑错误
    ```
    当前工作目录是 `E:\Code\ai-teaching-ppt`，而 MallJob 项目路径不正确。我注意到当前目录已经有 `.claude-coder` 和 `requirements.md`，说明当前项目是 `ai-teaching-ppt`。
    ```

# 回滚与非代码错误，导致的崩溃

    - 日志：
    ```
        ⠙ [Session 1] 编码中 22:25 | 读取文件
        前端代码已更新成功。数据库迁移让我用 Windows 方式执行：

        ⠦ [Session 1] 编码中 22:58 | 读取文件
        数据库迁移完成。现在进行测试验证：

        **Git 提交：** `8b31668`[INFO]  开始 harness 校验 ...
        [INFO]  ========== 开始校验 ==========
        [ERROR] session_result.json 解析失败: Expected ',' or '}' after property value in JSON at position 1002
        [OK]    检测到新提交: 8b31668 feat(feat-020): LLM 模型配置增强 - 支持温度、最大输入输出 token 设置
        [ERROR] ========== 校验失败 (致命) ==========
        [ERROR] Session 1 校验失败 (连续失败: 1/3)
        [WARN]  回滚到 febc49e9c923d75ef40ebbb70b6be5aa95145573 ...
        Unlink of file 'backend/ai_teaching_ppt.db' failed. Should I try again? (y/n) y
        Unlink of file 'backend/ai_teaching_ppt.db' failed. Should I try again? (y/n) y
        Unlink of file 'backend/ai_teaching_ppt.db' failed. Should I try again? (y/n) y
        Unlink of file 'backend/ai_teaching_ppt.db' failed. Should I try again? (y/n) n
        error: unable to unlink old 'backend/ai_teaching_ppt.db': Invalid argument
        Updating files: 100% (18/18), done.
        fatal: Could not reset index file to revision 'febc49e9c923d75ef40ebbb70b6be5aa95145573'.
        [ERROR] 回滚失败: Command failed: git reset --hard febc49e9c923d75ef40ebbb70b6be5aa95145573
    ```

    - 错误原因：
        未知

    - 出现了 " Unlink of file 'backend/ai_teaching_ppt.db' failed. Should I try again? (y/n) " 需要交互的东西，如何避免

    - reset 失败？ 是因为 session_result.json 错误？ 这个文件其实应该瘦身。确认这个文件目前的逻辑（貌似会组装 current\history?) 看看如何减少错误，即 让AI生成这个文件，历史组装，由代码来组合？

# 其它问题

    - 问题1：
        进程还是可能不会关闭，导致手动kill命令，才能释放 .next。 这里是否考虑修改步骤检查，或者在收尾阶段检查是否连续运行和达到运行次数，要求强制服务检查、关闭？

    - 问题2:
        使用阿里Coding Plan调用web_serach是不是没有任何效果，需要接入阿里云的 web_mcp?

    - 问题3：
        add 插入任务时，采用的是什么模型？

    - 问题4：
        默认跑5个，就会询问是否继续？这里是否应该修改，核心是 --max 参数, --pause 应该没有默认值，除非指定？

    - 问题5：
        在运行过程中展示当前时间 以及 运行时间，例如： "⠙ [Session 1] 14:25:30 编码中 22:25 | 读取文件"